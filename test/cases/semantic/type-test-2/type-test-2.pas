program test(input, output);
var
  p: array[1..10] of integer;
  i, j: integer;
  x, y: double;
  u, v: boolean;
function f(a:integer):integer;
begin
  f := input + output;
end;
begin
  i := f(1) and f(p[f(2)]);
  u := u + v;
  u := u or v;
  x := x mod p[i];
  y := p[y] mod i;
  i := i / j mod 2 + (2);
  u := i div j <> i * j;
  u := u > v;
  u := u <> v;
  i := -----i + j;
  x := ------x * -----u;
end.