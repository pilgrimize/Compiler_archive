#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#define STRING_SIZE 1000
const int i=2;
int a, gcd, b;
bool check;
int main(){
    if (b==0){
        a=1;
    }
    else {
        gcd=1;
    }
    b=23;
    if (a==0){
        
    }
    if (a==0){
        b=3;
    }
    else {
        b=2;
    }
    while (b<1){
        }
    while (a!=3){
        }
    do{
        printf("%s\n", "Hello, world!");
    }while(!(a!=b));
    do{
        printf("%s\n", "Hello, world!");
    }while(!(1>a));
    scanf("%d%d", &a, &b);
    printf("%s\n", "Hello, world!");
    return 0;
}
