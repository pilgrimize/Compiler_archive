program 234 Hello;
const i =2;
var a,gcd,b:integer;
check:boolean;
begin
	if b=0 then begin 
		a:=1
	end
	else gcd := 1;
    b := 23;
    if a=0 %% begin end;
    if a=0 then b:=3
    else b:=2;

    while b<1  begin
    end;

    while a<>3 & do begin
    end;

    repeat
        writeln('Hello, world!');
    until a<>b;
    
    repeat
        writeln('Hello, world!');
    1>a;

    readln & (a, + b);

    writeln ('Hello, world!');
end.